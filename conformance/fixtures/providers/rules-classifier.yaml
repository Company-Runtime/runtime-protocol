manifest:
  protocol: runtime/0.1
  provider: { id: rules-classifier, version: 1.0.0 }
  implements:
    - capability: reasoning.classify
      versions: ["^0.1"]
      traits: [batch]
      evidence: { claims: [execution], types: [provider_receipt] }
  credentials: { required: false, accepts: [] }
behavior:
  reasoning.classify:
    result: completed
    output: { labels: [{ id: billing, confidence: 0.92 }] }
    evidence:
      - {
          type: provider_receipt,
          claims: [execution],
          observed_at: "2026-01-01T00:00:01Z",
          data: { model: keyword-rules },
        }
