# `vendor` extensions

Vendor-specific definitions, one directory per vendor: `vendor/<vendor>/<capability-id>/`. Vendor names may appear only here and in adapter metadata.

No definitions are registered yet.
