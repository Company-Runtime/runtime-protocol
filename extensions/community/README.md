# `community` extensions

Community definitions, one directory per author: `community/<author>/<capability-id>/`. Identifiers are `community.<author>.<domain>.<verb>`.

No definitions are registered yet.
