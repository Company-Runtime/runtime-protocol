# `experimental` extensions

Proposals for the core vocabulary. Identifiers are `experimental.<domain>.<verb>`; on promotion the prefix is removed and the experimental identifier becomes a deprecated alias.

No definitions are registered yet.
