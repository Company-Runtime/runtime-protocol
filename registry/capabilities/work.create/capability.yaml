id: work.create
version: 0.1.0
status: experimental
domain: work
verb: create
description: Create a unit of work such as a task, ticket or issue.
input: { schema_ref: ./input.schema.json }
output: { schema_ref: ./output.schema.json }
profiles: []
traits:
  optional: [idempotency]
authority: { required: true }
evidence: { supported: true, claims: [execution, state] }
risk: { default: low }
effects: { mutating: true }
emits: [work.created]
relations:
  related: [work.assign, objective.create]
rfc: "0001"
