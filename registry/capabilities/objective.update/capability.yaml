id: objective.update
version: 0.1.0
status: experimental
domain: objective
verb: update
description: Revise an existing objective.
input: { schema_ref: ./input.schema.json }
output: { schema_ref: ./output.schema.json }
profiles: []
traits:
  optional: [idempotency]
authority: { required: true }
evidence: { supported: true, claims: [execution, state] }
risk: { default: low }
effects: { mutating: true }
emits: [objective.updated]
relations:
  related: [objective.create]
rfc: "0001"
