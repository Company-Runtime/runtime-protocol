id: work.cancel
version: 0.1.0
status: experimental
domain: work
verb: cancel
description: Stop a unit of work before it is completed.
input: { schema_ref: ./input.schema.json }
output: { schema_ref: ./output.schema.json }
profiles: []
traits:
  optional: [idempotency]
authority: { required: true }
evidence: { supported: true, claims: [execution, state] }
risk: { default: low }
effects: { mutating: true }
emits: [work.cancelled]
relations:
  related: [work.complete]
rfc: "0001"
