protocol: runtime/0.1
request_id: req_02
capability: { id: knowledge.search, version: "^0.1" }
actor: { ref: identity://agent/support-agent, type: agent }
input:
  query: How do I reset my password?
  limit: 3
constraints:
  timeout_ms: 5000
  regions: { allow: [eu] }
